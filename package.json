{
  "name": "finitestate",
  "version": "0.0.4",
  "description": "This SubQuery project can be use as a st...",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/contextfree-subql-starter",
  "repository": "github:subquery/contextfree-subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "qb",
  "license": "MIT",
  "devDependencies": {
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "^4.1.3"
  },
  "exports": {
    "chaintypes": "src/chaintypes.ts"
  },
  "dependencies": {
    "@polkadot/api": "^7"
  }
}
